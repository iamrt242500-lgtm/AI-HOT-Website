# Next.js EQS Demo

## Run

```bash
cd examples/nextjs-eqs-demo
npm install
npm run dev
```

Set env if needed:

- `NEXT_PUBLIC_COLLECTOR_ENDPOINT` (default: `http://localhost:8081/v1/events`)
- `NEXT_PUBLIC_SITE_ID` (default: `nextjs-demo-site`)
